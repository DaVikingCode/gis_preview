// Shared so the tour step and the swipe overlay maps agree on the initial view.
export const SWIPE_VIEW: { center: [number, number]; zoom: number } = {
  center: [2.349, 48.853], // Paris — fort contraste 1950s ↔ aujourd'hui
  zoom: 15,
}
